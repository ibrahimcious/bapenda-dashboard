services:
  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_USER: tanstack
      POSTGRES_PASSWORD: tanstack
      POSTGRES_DB: tanstack
    ports:
      - "5678:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
